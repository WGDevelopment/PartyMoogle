{
  "//": "Copy this file to 'appsettings.Local.json' (same folder) and fill in your real values. That file is gitignored, so your key never gets committed. Anything you put here overrides appsettings.json. You usually only need the API key — BaseUrl and the Screenpipe path already default correctly.",

  "Llm": {
    "ApiKey": "PUT-YOUR-GPU_LLM_KEY-HERE",
    "Model": "qwen2.5-vl-7b-instruct"
  }
}
