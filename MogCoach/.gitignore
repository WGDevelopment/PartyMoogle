# .NET
bin/
obj/
*.user
*.suo
.vs/

# Build / restore
artifacts/
[Dd]ebug/
[Rr]elease/
project.lock.json

# Local config with secrets — copy appsettings.json to appsettings.Local.json
appsettings.Local.json
appsettings.*.Local.json
.env

# Captured sessions (raw telemetry + frames) and generated reports
captures/
reports/
*.mogcap

# ...but DO keep the bundled sample captures — they're smoke-test fixtures, not real
# captures. These negations override the *.mogcap rule above and the repo-root *.log rule.
!samples/sample-session.mogcap
!samples/sample-capture.log

# OS
.DS_Store
Thumbs.db
